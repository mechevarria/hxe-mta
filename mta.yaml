ID: hxe_mta
_schema-version: '2.1'
version: 0.0.1

modules:
 - name: demo_db
   type: hdb
   path: demo_db
   requires:
    - name: hdi_demo_db

resources:
 - name: hdi_demo_db
   parameters:
      config:
         schema: demo_DB
   properties:
      hdi-container-name: ${service-name}
   type: com.sap.xs.hdi-container
